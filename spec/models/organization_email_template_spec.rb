require 'rails_helper'

RSpec.describe OrganizationEmailTemplate, "Creating an organization email template" do
  context "When an organization email template is created with the default Email Type settings" do
    # before(:each) do
    #   @email_type = OrganizationEmailType.first
    #
    #   @template = @email_type.organization_email_templates.new
    # end
    #
    # it "should have the default introduction text" do
    #
    #   expect(@template.introduction_text).to eq(@email_type.introduction_text)
    # end
    #
    # it "should have the default conclusion text" do
    #
    #   expect(@template.conclusion_text).to eq(@email_type.conclusion_text)
    # end
    #
    # it "should have the default body item" do
    #   expect(@template.marketing_materials).to eq(@email_type.marketing_materials)
    # end
  end

end
